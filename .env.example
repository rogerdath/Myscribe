# Required for audio transcription (TikTok, Facebook, YouTube without captions)
OPENAI_API_KEY=sk-...

# Set to "true" to return mock data without real API calls (development only)
MOCK_TRANSCRIPTION=false
